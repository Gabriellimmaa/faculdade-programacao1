#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

void pegando_frase(frase){
    retunr
}

int main() {
    char frase[400];
    scanf("%[^\n]s", &frase);
    printf("%s", strrchr(frase, ' ') + 1);

	return 0;
}
